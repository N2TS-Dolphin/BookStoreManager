<Window x:Class="BookStoreManager.AddOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookStoreManager"
        mc:Ignorable="d"
        Title="AddOrderWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="15"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1.2*"/>
            <RowDefinition Height="5"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ADD ORDER" FontWeight="Bold" FontSize="20" Margin="5" Grid.Column="2" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Vertical" Margin="5" VerticalAlignment="Center">
            <!-- StackPanel for Customer Name -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Label Width="100" Content="Customer Name:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtCustomerName" Width="200" Margin="5" VerticalAlignment="Center"/>
            </StackPanel>
            <!-- StackPanel for Order Date -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Label Width="100" Content="Order Date:" VerticalAlignment="Center"/>
                <DatePicker x:Name="dpOrderDate" Width="200" Margin="5" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <Button x:Name="AddOrderBtn"  Grid.Row="3" Grid.Column="2" Width="55" Height="25" Content="Add" Click="AddOrderBtn_Click"/>
    </Grid>
</Window>
